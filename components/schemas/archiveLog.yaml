type: object
description: |
  The archive log of the Spark application.
properties:
  logsArchiveUrl:
    type: string
    format: uri
    description: |
      The URL of the archive log of the Spark application.
    example: string
  availability:
    type: string
    description: |
      The availability of the archive log of the Spark application.
    example: PENDING
