type: object
properties:
  instanceTypes:
    type: array
    description: >
      The allowed Virtual Node Group's instance types when instance types
      filters is set.
    items:
      type: string
    example:
      - c3.large
      - c5.large
