type: object
properties:
  launchSpecIds:
    type: array
    description: >
      List of virtual node group identifiers to be rolled. Each identifier is a
      string. Can be null. Cannot be used together with instanceIds.
    items:
      type: string
      example: ols-76694a7d, ols-56694a76
