type: object
required:
  - roll
properties:
  roll:
    type: object
    description: |
      Object that specifies the roll parameters to update.
    required:
      - status
    properties:
      status:
        type: string
        enum:
          - STOPPED
        description: |
          Set roll status. The only valid value is "STOPPED".
        example: STOPPED
