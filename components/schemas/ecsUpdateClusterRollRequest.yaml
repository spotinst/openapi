type: object
required:
  - roll
properties:
  roll:
    type: object
    required:
      - status
    properties:
      status:
        type: string
        example: STOPPED
        description: |
          Valid values: "STOPPED"
