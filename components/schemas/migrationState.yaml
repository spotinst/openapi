type: string
description: |
  The latest state of this migration.
enum:
  - STARTING
  - AWAIT_REPLACEMENTS
  - HANDLE_UNSCHEDULE
  - DRAIN_NODES
  - VALIDATE_DRAIN
  - STOPPING
  - ERROR
  - RESCHEDULE_NODES
  - TERMINATE_NODES
  - AWAIT_TERMINATION
  - FINISHED_ERROR
  - STOPPED
  - FINISHED
example: FINISHED
