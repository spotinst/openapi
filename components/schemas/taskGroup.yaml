type: object
title: Task Group Object
properties:
  instanceTypes:
    $ref: ./instanceTypes.yaml
  capacity:
    $ref: ./capacity.yaml
  lifeCycle:
    $ref: ./lifecycle.yaml
    example: SPOT
    description: |
      **required** only with `clone` or `new` strategy
  ebsConfiguration:
    $ref: ./ebsConfiguration.yaml
  configurations:
    oneOf:
      - $ref: ./jsonConfiguration.yaml
      - $ref: ./configurations.yaml
