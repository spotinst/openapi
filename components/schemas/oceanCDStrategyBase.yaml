type: object
title: Ocean CD Strategy
description: >
  Ocean CD strategy. Use in RolloutSpec to decide what strategy to use when
  applying `SpotDeployment`
properties:
  canary:
    $ref: ./oceanCDStrategyCanary.yaml
required:
  - name
  - canary
