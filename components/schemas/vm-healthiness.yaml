type: object
properties:
  vmName:
    type: string
    description: Vm Name
    example: vm-123456789
  lifeCycle:
    type: string
    description: Life Cycle
    enum:
      - SPOT
      - ON_DEMAND
    example: SPOT
  healthStatus:
    type: string
    description: Health Status
    enum:
      - HEALTHY
      - UNHEALTHY
      - UNKNOWN
    example: HEALTHY
