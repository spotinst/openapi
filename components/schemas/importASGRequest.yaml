type: object
properties:
  launchSpec:
    type: object
    description: |
      The object specifying the configuration of the VNG.
    properties:
      instanceTypes:
        type: array
        description: |
          List of possible instance types for the cluster.
        items:
          type: string
        example:
          - c3.large
          - m4.large
