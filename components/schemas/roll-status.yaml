type: object
properties:
  status:
    type: string
    description: |
      Sets the status of the roll.
    example: IN_PROGRESS
    enum:
      - IN_PROGRESS
      - COMPLETED
      - FAILED
      - STOPPED
