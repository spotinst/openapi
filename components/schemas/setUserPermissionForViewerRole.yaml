type: object
title: Set user permissions for editor role
properties:
  userEmail:
    type: string
    description: One of userEmail / userId is required. identify user by its user ID
    example: test@spot.io
  role:
    type: string
    description: >-
      Valid values - "viewer", "editor". Required on permissionStrategy =
      ROLE_BASED
    example: editor
