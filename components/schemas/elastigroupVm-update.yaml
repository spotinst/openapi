type: object
title: Update Elasitgroup Azure Spot VMs - Request Available fields
required:
  - group
properties:
  group:
    type: object
    title: Azure Elastigroup
    properties:
      name:
        type: string
        example: spotinst-group
        description: |
          Azure Elastigroup Name
      description:
        type: string
        example: This is my example elastigroup
        description: |
          Describe your Azure Elastigroup
      capacity:
        $ref: ./properties-capacity.yaml
      strategy:
        $ref: ./elastigroupVm-strategy.yaml
      scaling:
        $ref: ./elastigroupVm-scaling.yaml
      compute:
        $ref: ./elastigroupVm-compute-update.yaml
      health:
        $ref: ./elastigroupVm-health.yaml
      scheduling:
        $ref: ./elastigroupVm-scheduling.yaml
      createdAt:
        type: string
        readOnly: true
        format: date-time
        example: 2018-10-10T10:50:29.000+0000
      updatedAt:
        type: string
        readOnly: true
        format: date-time
        example: 2018-11-01T10:50:29.000+0000
