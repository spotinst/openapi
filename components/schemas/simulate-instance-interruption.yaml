type: object
properties:
  instancesToInterrupt:
    type: array
    description: |
      A list of group's instance ids to interrupt
    items:
      type: string
    example:
      - i-123456
      - i-9876543
