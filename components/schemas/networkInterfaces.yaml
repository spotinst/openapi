title: Network Interfaces array
type: array
items:
  type: object
  required:
    - network
  properties:
    network:
      type: string
      example: spot-network
    projectId:
      type: string
      example: test-project
