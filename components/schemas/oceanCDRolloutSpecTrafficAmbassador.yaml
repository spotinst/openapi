type: object
title: Ocean CD Rollout Spec Traffic Ambassador
description: |
  Holds specific configuration to use Ambassador to route traffic
properties:
  mappings:
    description: >-
      A list of names of the Ambassador Mappings used to route traffic to the
      service
    type: array
    items:
      type: string
      description: The name of the Ambassador Mappings used to route traffic to the service
      example: stable-mapping
required:
  - mappings
