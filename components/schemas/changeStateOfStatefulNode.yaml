type: object
title: Change State Of Stateful Node
properties:
  state:
    type: string
    enum:
      - pause
      - resume
      - recycle
    title: Stateful Node State Change
    example: pause
