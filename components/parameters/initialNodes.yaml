in: query
name: initialNodes
example: 1
description: >
  When set to an integer greater than 0, a corresponding number of nodes will be
  launched from the  virtual node group created.
schema:
  type: integer
  default: null
