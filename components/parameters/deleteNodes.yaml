in: query
name: deleteNodes
example: true
description: >
  When set to "true", all instances belonging to the deleted launch
  specification will be drained, detached, and terminated.
schema:
  type: boolean
  default: false
