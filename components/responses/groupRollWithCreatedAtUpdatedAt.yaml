description: Elastigroup Response
content:
  application/json:
    schema:
      allOf:
        - $ref: ../schemas/responseItemWrapper.yaml
        - type: object
          properties:
            response:
              type: object
              properties:
                items:
                  type: array
                  items:
                    allOf:
                      - $ref: ../schemas/roll.yaml
                      - $ref: ../schemas/createdAtUpdatedAt.yaml
                kind:
                  example: spotinst:gcp:gce:group:roll
