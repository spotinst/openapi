put:
  summary: Swap OS Disk to Stateful Node
  description: Configure a new managed OS disk for an OS persisted paused Stateful Node.
  operationId: azureStatefulNodeSwapOSDisk
  tags:
    - Elastigroup Azure Stateful
  parameters:
    - $ref: ../components/parameters/accountId.yaml
    - $ref: ../components/parameters/nodeId.yaml
  requestBody:
    required: true
    content:
      application/json:
        schema:
          allOf:
            - $ref: ../components/schemas/swapOsDiskToStatefulNode.yaml
  responses:
    '200':
      description: Ok
    '400':
      description: Bad Request
