put:
  summary: User - Update Policy Mapping
  description: |
    Update the mapping of a given user directly to policies
  operationId: userUpdatePolicyMapping
  tags:
    - Organizations
  parameters:
    - $ref: ../components/parameters/userId.yaml
  requestBody:
    required: true
    content:
      application/json:
        schema:
          allOf:
            - $ref: ../components/schemas/updatePolicyMapping.yaml
  responses:
    '200':
      $ref: ../components/responses/updatePolicyMapping.yaml
    '400':
      description: Bad Request
