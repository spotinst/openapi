get:
  summary: Get Fargate Services Discovery
  description: |
    Get existing Fargate services in the ECS cluster.
  operationId: oceanEcsFargateGetServicesDiscovery
  tags:
    - Ocean ECS
  parameters:
    - $ref: ../components/parameters/accountId.yaml
    - $ref: ../components/parameters/oceanClusterId.yaml
  responses:
    '200':
      $ref: ../components/responses/fargate-discovery.yaml
    '400':
      description: Bad Request
