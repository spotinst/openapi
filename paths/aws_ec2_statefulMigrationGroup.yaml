post:
  summary: Import Stateful Instance
  description: >
    Create a new Stateful Elastigroup – this Elastigroup will have the same
    configuration and state of the imported instance.
  operationId: importStatefulInstance
  tags:
    - Elastigroup AWS
  parameters:
    - $ref: ../components/parameters/accountId.yaml
  requestBody:
    required: true
    content:
      application/json:
        schema:
          allOf:
            - $ref: ../components/schemas/importStatefulInstance.yaml
  responses:
    '200':
      $ref: ../components/responses/importStatefulInstance.yaml
    '400':
      description: Bad Request
