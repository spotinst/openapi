put:
  summary: Update Stateful Node States
  description: |
    Update the state of multiple Stateful Nodes.
  operationId: AWSManagedInstanceUpdateStates
  tags:
    - Stateful Node AWS
  parameters:
    - $ref: ../components/parameters/accountId.yaml
  requestBody:
    required: true
    content:
      application/json:
        schema:
          allOf:
            - $ref: ../components/schemas/updateManagedInstanceStatesRequest.yaml
  responses:
    '200':
      $ref: ../components/responses/updateManagedInstanceStatesResponse.yaml
    '400':
      description: Bad Request
