allOf:
  - $ref: "../schemas/responseWrapper.yaml"
  - type: "object"
    properties:
      response:
        type: "object"
        properties:
          errors:
            type: "array"
            description: List of errors.
            items:
              $ref: "../schemas/errorModel.yaml"
