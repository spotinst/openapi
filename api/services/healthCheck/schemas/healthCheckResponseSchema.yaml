type: object
properties:
  id:
    type: integer
    example: 1
  resourceId:
    type: string
    example: sig-12345
  name:
    type: string
    example: Service-1-healthCheck
  proxyAddress:
    type: string
    example: 51.22.125.47
  proxyPort:
    type: integer
    example: 80
  check:
    type: object
    properties:
      protocol:
        type: string
        example: http
      port:
        type: integer
        example: 80
      endpoint:
        type: string
        example: index.html
      unhealthyThreshold:
        type: integer
        example: 2
      healthyThreshold:
        type: integer
        example: 2
      interval:
        type: integer
        example: 30
      timeout:
        type: integer
        example: 60
