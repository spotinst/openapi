post:
  summary: "Organization & Accounts - Set Cloud Credentials For Azure"
  description: Link a Spotinst account to an Azure Cloud account.
  operationId: "OrganizaionAndAccountSetCloudCredentialsForAzure"
  tags:
    - "Organization & Accounts"
  parameters:
    - $ref: "../../../../commons/parameters/accountId.yaml"
  requestBody:
    required: true
    content:
      application/json:
        schema:
          allOf:
            - $ref: "../schemas/setCloudCredentialsForAzure.yaml"
  responses:
    200:
      $ref: "../responses/setCloudCredentialsForAzure.yaml"
    400:
      description: "Bad Request"