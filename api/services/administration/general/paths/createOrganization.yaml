post:
  summary: "Create Organization"
  description: Create a Spot Organization.
  operationId: "OrganizationsAndAccountsCreateOrganization"
  tags:
    - "Organizations"
  requestBody:
    required: true
    content:
      application/json:
        schema:
          allOf:
            - $ref: "../schemas/createOrganization.yaml"
  responses:
    200:
      $ref: "../responses/createOrganization.yaml"
    400:
      description: "Bad Request"
