post:
  summary: "Organization & Accounts - Create Organization"
  description: Create a Spotinst Organization.
  operationId: "OrganizaionAndAccountCreateOrganization"
  tags:
    - "Organization & Accounts"
  requestBody:
    required: true
    content:
      application/json:
        schema:
          allOf:
            - $ref: "../schemas/createOrganization.yaml"
  responses:
    200:
      $ref: "../responses/createOrganization.yaml"
    400:
      description: "Bad Request"