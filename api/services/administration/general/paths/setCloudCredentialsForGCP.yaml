post:
  summary: "Set Cloud Credentials for GCP"
  description: Link a Spot account to an GCP Cloud account.
  operationId: "OrganizationsAndAccountsSetCloudCredentialsForGCP"
  tags:
    - "Organizations and Accounts"
  parameters:
    - $ref: "../../../../commons/parameters/accountId.yaml"
  requestBody:
    required: true
    content:
      application/json:
        schema:
          allOf:
            - $ref: "../schemas/setCloudCredentialsForGCP.yaml"
  responses:
    200:
      $ref: "../responses/setCloudCredentialsForGCP.yaml"
    400:
      description: "Bad Request"
