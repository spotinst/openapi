post:
  summary: "Organization & Accounts - Set Cloud Credentials For GCP"
  description: Link a Spotinst account to an GCP Cloud account.
  operationId: "OrganizaionAndAccountSetCloudCredentialsForGCP"
  tags:
    - "Organization & Accounts"
  parameters:
    - $ref: "../../../../commons/parameters/accountId.yaml"
  requestBody:
    required: true
    content:
      application/json:
        schema:
          allOf:
            - $ref: "../schemas/setCloudCredentialsForGCP.yaml"
  responses:
    200:
      $ref: "../responses/setCloudCredentialsForGCP.yaml"
    400:
      description: "Bad Request"