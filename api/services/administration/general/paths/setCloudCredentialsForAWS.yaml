post:
  summary: "Organization & Accounts - Set Cloud Credentials For AWS"
  description: Link a Spotinst account to an AWS Cloud account.
  operationId: "OrganizaionAndAccountSetCloudCredentialsForAWS"
  tags:
    - "Organization & Accounts"
  parameters:
    - $ref: "../../../../commons/parameters/accountId.yaml"
  requestBody:
    required: true
    content:
      application/json:
        schema:
          allOf:
            - $ref: "../schemas/setCloudCredentialsForAWS.yaml"
  responses:
    200:
      $ref: "../responses/setCloudCredentialsForAWS.yaml"
    400:
      description: "Bad Request"