type: object
properties:
  userIds:
    type: array
    description: The users to register under the given user group (should be existing users only)

example:
  userIds: [ "u-12345678", "pu-12345678" ]