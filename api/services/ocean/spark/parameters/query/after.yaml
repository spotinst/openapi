in: query
name: after
required: false
schema:
  $ref: "../../schemas/after.yaml"
description: >
  If specified, the page returned in the response contains the items right after this cursor in the ordering
