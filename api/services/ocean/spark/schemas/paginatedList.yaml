type: object
required:
  - page
  - total
properties:
  total:
    type: integer
    description: The total number of items in the collection
    example: 3256
  page:
    type: array
    description: The items of the current page
    example: []
  after:
    $ref: "after.yaml"
  before:
    $ref: "before.yaml"
