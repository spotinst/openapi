summary: Ocean Spark

put:
  summary: "Stop Workspace"
  description: >
    All Ocean Spark configuration parameters are updatable. This API supports partial updates, so specific fields can be updated separately.
  operationId: "OceanSparkWorkspaceStop"
  tags:
    - "Ocean Spark"
  parameters:
    - $ref: "../parameters/path/oceanSparkWorkspaceId.yaml"
    - $ref: "../../../../commons/parameters/accountId.yaml"
  requestBody:
    required: true
    content:
      application/json:
        schema:
          allOf:
            - $ref: "../schemas/oceanSparkWorkspaceStopWrapper.yaml"
  responses:
    200:
      $ref: "../responses/workspaces.yaml"
    400:
      description: "Bad Request"
