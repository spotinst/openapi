description: >
  App metrics over time grouped by the selected grouping and ranked.
content:
  application/json:
    schema:
      allOf:
        - $ref: "../../../../commons/schemas/responseItemWrapper.yaml"
        - type: object
          properties:
            response:
              type: object
              description: >
                response body
              properties:
                items:
                  type: array
                  items:
                    $ref: "../schemas/clusterAppUsageMetricsOverTime.yaml"
