get:
  summary: "Get Cluster Nodes"
  description: >
    Get nodes data of an Ocean cluster.
  operationId: "oceanGkeGetNodes"
  tags:
    - "Ocean GKE"
  parameters:
    - $ref: "../../../../commons/parameters/accountId.yaml"
    - in: path
      name: "oceanClusterId"
      schema:
        type: string
      required: true
      example: o-1abcd124
      description: >
        The identifier of the Ocean cluster
    - in: query
      name: "launchSpecId"
      schema:
        type: string
      example: ols-123
      description: Filter nodes by launch spec id
    - in: query
      name: "instanceName"
      schema:
        type: string
      example: sin-123
      description: Get a specific node
  responses:
    200:
      $ref: "../responses/clusterNodesResponse.yaml"
    400:
      description: "Bad Request"