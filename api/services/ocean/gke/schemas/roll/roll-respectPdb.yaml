type: object
properties:
  respectPdb:
    type: boolean
    description: >
      During the roll, if the parameter is set to 'true', PDB is honored during the instance replacement.
    example: true
    default: false
