type: object
properties:
  roll:
    type: object
    required:
      - status
    description: >
      Sets the status of the roll.
    properties:
      status:
        type: string
        enum: [STOPPED]
        description: >
          Set status of a roll. Valid values: "STOPPED"
        example: STOPPED