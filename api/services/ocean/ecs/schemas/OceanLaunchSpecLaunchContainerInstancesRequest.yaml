type: object
properties:
  launchRequest:
    type: object
    title: Ocean ECS VNG Launch Request
    required:
      - amount
    properties:
      amount:
        description: |
          The number of container instances to launch.
        type: integer
        example: 5
