type: object
properties:
  launchRequest:
    type: object
    title: Ocean ECS Launch Spec Launch Request
    required:
      - amount
    properties:
      amount:
        description: |
          The amount of container instances to launch.
        type: integer
        example: 5