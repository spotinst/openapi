get:
  summary: "Ocean ECS - Get Cluster"
  description: >
    Get the configurations for a specified Ocean cluster.
  operationId: "oceanEcsGetCluster"
  tags:
    - "Ocean ECS"
  parameters:
    - $ref: "../../../../commons/parameters/accountId.yaml"
    - $ref: "../../commons/parameters/oceanClusterId.yaml"
  responses:
    200:
      $ref: "../responses/ocean.yaml"
    400:
      description: "Bad Request"

put:
  summary: "Ocean ECS - Update Cluster"
  description: >
    All Ocean parameters are updatable, excluding the Region and ID.
    This API supports partial updates, so specific fields can be updated separately.
  operationId: "oceanEcsUpdateCluster"
  tags:
    - "Ocean ECS"
  parameters:
    - $ref: "../../commons/parameters/oceanClusterId.yaml"
    - $ref: "../../../../commons/parameters/accountId.yaml"
    - $ref: "../../../../commons/parameters/autoApplyTags.yaml"
  requestBody:
    required: true
    content:
      application/json:
        schema:
          allOf:
          - $ref: "../schemas/ocean.yaml"
  responses:
    200:
      $ref: "../responses/ocean.yaml"
    400:
      description: "Bad Request"

delete:
  summary: "Ocean ECS - Delete Cluster"
  description: >
    Delete a specified Ocean cluster.
  operationId: "oceanEcsDeleteCluster"
  tags:
    - "Ocean ECS"
  parameters:
    - $ref: "../../../../commons/parameters/accountId.yaml"
    - $ref: "../../commons/parameters/oceanClusterId.yaml"
  responses:
    200:
      $ref: "../../../../commons/responses/spotinstResponseWrapper.yaml"
    400:
      description: "Bad Request"
