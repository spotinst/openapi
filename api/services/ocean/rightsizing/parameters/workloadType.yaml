in: path
name: workloadType
schema:
  type: string
  enum:
    - deployment
    - daemonSet
    - statefulSet
required: true
example: deployment
description: >
  Workload type as defined in kubernetes.

