in: path
name: workloadType
schema:
  type: string
  enum:
    - deployment
    - daemonSet
    - statefulSet
    - rollout
required: true
example: deployment
description: >
  Workload type as defined in kubernetes.

