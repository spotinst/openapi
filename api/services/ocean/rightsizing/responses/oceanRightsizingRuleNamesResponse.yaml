description: Ocean Rightsizing Deleted Rule Names Response
content:
  application/json:
    schema:
      type: object
      properties:
        kind:
          type: string
          example: "spotinst:ocean:rightsizing:rule"
          #    todo shir - rightsizing ? not rightSizing. also appears "ocean:k8s.." in jira
        items:
          type: object
          properties:
            ruleNames:
              type: array
              example: ["dev-rule", "test-rule"]
              items:
                type: string
