type: object
required:
  - config
properties:
  config:
    type: object
    required:
      - recommendationsPercentile
    properties:
      recommendationsPercentile:
        type: integer
        description: Change the percentile for which our recommendations calculation will need to take into consideration.
        example: 85
        enum: [ 85, 90, 95, 99 ]