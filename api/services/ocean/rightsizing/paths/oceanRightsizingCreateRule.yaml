post:
  summary: "Create Ocean Rightsizing Rule"
  description: |
  operationId: "oceanRightsizingCreateRule"
  # todo shir - on operationId the REST operation should be the last word - "oceanRightsizingRuleCreate"
  tags:
    - "Ocean Rightsizing"
  parameters:
    - $ref: "../parameters/oceanId.yaml"
    - $ref: "../../../../commons/parameters/accountId.yaml"
  requestBody:
    required: true
    content:
      application/json:
        schema:
          allOf:
            - $ref: "../schemas/oceanRightsizingRuleRequest.yaml"

  responses:
    200:
      $ref: "../responses/oceanRightsizingRuleResponse.yaml"
    400:
      description: "Bad Request"