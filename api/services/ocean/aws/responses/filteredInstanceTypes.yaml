description: Ocean Instance Types Filters Simulation Response
#todo guy barman adjust description to title - done
content:
  application/json:
    schema:
      allOf:
        - $ref: "../../../../commons/schemas/responseItemWrapper.yaml"
        - type: object
          properties:
            request:
              type: object
              properties:
                url:
                  example: "/ocean/aws/k8s/cluster/o-abcd1234/instanceTypeFiltersSimulation"
                method:
                  example: "POST"
            response:
              type: object
              properties:
                items:
                  type: array
                  items:
                    $ref: "../schemas/filteredInstanceTypes.yaml"
                kind:
                  example: "spotinst:ocean:aws:k8s:instanceTypesFilters:simulation"
                  # todo guy barman :simulation:filteredInstanceType. yinnon said that the new suggestion is better, but he prefers that we will stay with the current KIND so we will not ask for snit another approval.