type: object
properties:
  respectPdb:
    type: boolean
    description: >
      During the roll, if the parameter is set to True we honor PDB during the instance replacement.
    example: true
    default: false
