type: object
description: Determines the utilization of instance store volumes. If not defined, instance store volumes will not be used.
properties:
  type:
    type: string
    description: |
        The method for using the instance store volumes (**must also be defined in the userData**).
    enum: ["RAID0"]
    example: RAID0
