type: object
properties:
  respectPdb:
    type: boolean
    description: >
      During the migration, if the parameter is set to true we honor PDB during the nodes replacement.
    example: false
    default: true
