summary: Ocean CD
get:
  summary: "Get Ocean CD Cluster"
  description: >
    Get the configuration of an existing Ocean CD cluster.
  operationId: "OceanCDClusterGet"
  tags:
  - "Ocean CD"
  parameters:
    - in: "path"
      name: "clusterId"
      schema:
        type: "string"
      required: true
      example: TestCluster
      description: >
        The name identifier of the Ocean CD cluster
  responses:
    200:
      $ref: "../responses/oceanCDClusterResponse.yaml"
    400:
      description: "Bad Request"
delete:
  summary: "Delete Ocean CD Cluster"
  description: >
    Delete an existing Ocean CD cluster.
  operationId: "OceanCDClusterDelete"
  tags:
  - "Ocean CD"
  parameters:
    - in: "path"
      name: "clusterId"
      schema:
        type: "string"
      required: true
      example: TestCluster
      description: >
        The name identifier of the Ocean CD cluster
  responses:
    200:
      description: "Ok"
    400:
      description: "Bad Request"
put:
  summary: "Update Ocean CD Cluster"
  description: >
    Update Ocean CD cluster parameters.
  operationId: "OceanCDClusterUpdate"
  tags:
    - "Ocean CD"
  parameters:
    - in: "path"
      name: "clusterId"
      schema:
        type: "string"
      required: true
      example: testCluster
      description: >
        The identifier of the Ocean CD cluster
  requestBody:
    required: true
    content:
      application/json:
        schema:
          allOf:
            - $ref: "../schemas/oceanCDClusterNotification.yaml"
  responses:
    200:
      description: "Ok"
    400:
      description: "Bad Request"