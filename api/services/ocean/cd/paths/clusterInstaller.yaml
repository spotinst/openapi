summary: Ocean CD
post:
  summary: "Install OceanCD cluster"
  description: >
    Create SAAS Ocean CD cluster entity
  operationId: "OceanCdClusterCreate"
  tags:
    - "Ocean CD"
  parameters:
    - in: "query"
      name: "clusterId"
      example: "testCluster"
      description: >
        Cluster Id
      schema:
        type: "string"
      required: true
  responses:
    200:
      $ref: "../responses/oceanCDInstallerResponse.yaml"
    400:
      description: "Bad Request"