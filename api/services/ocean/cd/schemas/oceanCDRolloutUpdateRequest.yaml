type: object
title: Ocean CD Cluster Rollout Update Request
description: >
  Ocean CD cluster rollout update request entity.
properties:
  action:
    type: string
    enum: [rollback]
required:
  - action