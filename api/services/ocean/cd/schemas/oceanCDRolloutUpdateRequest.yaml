type: object
title: Ocean CD Rollout Update Request
description: >
  Ocean CD rollout update request entity.
properties:
  action:
    type: string
    description: An action that might be applied to a rollout
    enum: [promote, promoteFull, pause, abort, retry]
required:
  - action