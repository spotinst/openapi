type: object
title: Ocean CD Verification Template Secret Key Ref
description: Secret key to use
required:
  - name
  - key
properties:
  name:
    description: Name of the secret key
    example: root-password-secret
    type: string
  key:
    description: Name of the field that stores data
    example: root-password
    type: string