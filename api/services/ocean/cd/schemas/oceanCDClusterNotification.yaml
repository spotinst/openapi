type: object
required:
  - notification
properties:
  minutesWithoutHeartbeat:
    type: double
    example: 4
    description: >
      Number of minutes without receiving cluster controller heartbeat
  providers:
    type: array
    description: |
      List of notification provider names
    items:
      type: string
      example: "provider1"