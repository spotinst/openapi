type: object
title: Ocean CD Rollout Spec Failure Policy
description: >
  Holds information on how to react when failure happens
properties:
  action:
    type: string
    description: Choose an action to perform on failure
    enum: [abort]
    example: "abort"
required:
  - action