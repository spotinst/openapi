type: object
properties:
  batchSizePercentage:
    type: integer
    description: >
      Value as a percent to set the size of a batch in a migration. Valid values are 0-100.
      In case of null as value, the default value in the backend will be 20%.
    example: 25
