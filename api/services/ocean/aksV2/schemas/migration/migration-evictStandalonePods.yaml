type: object
properties:
  terminateNodes:
    type: boolean
    description: >
      If the parameter is set to true we will evict all standalone pods during the migration.
    example: true
    default: true
