type: object
properties:
  nodePoolNames:
    type: array
    description: >
      List of node pools to be migrated.
      Each node pool name is a string. nodePoolNames can be null, and cannot be used together with nodeNames.
    items:
      type: string
      example:
        - omnp35f6cccb
        - omnp55bd559b
