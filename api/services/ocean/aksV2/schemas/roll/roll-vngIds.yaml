type: object
properties:
  vngIds:
    type: array
    description: >
      List of virtual node group identifiers to be rolled.
      Each identifier is a string. vngIds can be null, and cannot be used together with nodeNames and nodePoolNames.
    items:
      type: string
      example:
        - vng-69dc457b
        - vng-175947a4
