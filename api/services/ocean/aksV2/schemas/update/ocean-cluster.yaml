type: object
description: |
  The Ocean cluster.
properties:
  name:
    type: string
    example: test
    description: |
      A name for the Ocean cluster.
  autoScaler:
    $ref: "../../schemas/ocean-autoScaler.yaml"
  scheduling:
    $ref: "../../schemas/ocean-scheduling.yaml"
  health:
    $ref: "../../schemas/ocean-health.yaml"
  virtualNodeGroupTemplate:
    $ref: "../../schemas/update/ocean-virtualNodeGroupTemplate.yaml"
