in: query
name: "forceDelete"
example: true
default: null
description: >
  When set to "true", the launch spec will be deleted even if useAsTemlateOnly set to true and it's the only launch spec of the cluster.
schema:
  type: boolean
required: false

