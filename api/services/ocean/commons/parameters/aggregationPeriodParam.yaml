in: "query"
name: "aggregationPeriod"
schema:
  type: "string"
required: false
description: >
  Aggregation period may be "daily" or null. If you use "daily" the costs will be broken into parts, e.g., the cost per each day in the period. If you don't pass the aggregation period, there will be only one cost per resource for the whole period.
