post:
  summary: "Instance Unlock"
  description: >
    Remove the termination protection from specific instance
  operationId: "awsElastigroupInstanceUnlock"
  tags:
    - "Elastigroup AWS"
  parameters:
    - $ref: "../../aws/parameters/instanceId.yaml"
    - $ref: "../../../../commons/parameters/accountId.yaml"
  responses:
    200:
      $ref: "../responses/instance-unlock.yaml"
    400:
      description: "Bad Request"
