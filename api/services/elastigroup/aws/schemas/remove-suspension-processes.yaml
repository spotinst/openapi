type: object
title: Metadata Options Update
description: Request - Example 6 - Update Metadata Options

properties:
  group:
    type: object
    title: AWS Elastigroup

    properties:
      compute:
        type: object
        name: Compute Attribute
        title: Compute Attribute

        properties:
          launchSpecification:
            type: object
            name: Launch Specification Attribute
            title: Launch Specification Attribute

            properties:
              metadataOptions:
                type: object
                name: Metadata Options Attribute
                title: Metadata Options Attribute

                properties:
                  httpTokens:
                    type: string
                    example: "optional"
                    description: >
                      Http Token