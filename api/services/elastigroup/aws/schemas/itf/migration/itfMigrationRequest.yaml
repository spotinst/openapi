title: Listener Rule Migration Request
type: object
properties:
  status:
    type: string
    description: Retry migration process of specific rule in "Failed" status.
    enum: [ "USER_RETRY" ]
    example: "USER_RETRY"