get:
  description: "Get the details of the selected Ocean-CCO integration."
  operationId: get_flexera_cco_integration
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: "../../schemas/"