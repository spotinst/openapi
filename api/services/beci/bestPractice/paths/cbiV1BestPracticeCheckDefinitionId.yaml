get:
  tags:
    - Best Practice Check Definitions
  summary: Get Check Definition
  description: Returns a check definitions details.
  operationId: getCheckDefinitionCbiV1BestPracticeCheckDefinitionCheckIdGet
  parameters:
    - name: checkId
      in: path
      required: true
      schema:
        type: string
        description: Check ID
      description: Check ID
  responses:
    '200':
      $ref: ../responses/get200cbiV1BestPracticeCheckDefinitionId.yaml
    '400':
      content:
        application/json:
          schema:
            $ref: ../../../../commons/schemas/errorResponse.yaml
      description: Invalid request error response. Adjust the request before retrying.
    '404':
      content:
        application/json:
          schema:
            $ref: ../../../../commons/schemas/errorResponse.yaml
      description: Not found error response. The requested resource does not exist.
    '422':
      description: Error response.
      content:
        application/json:
          schema:
            $ref: ../../../../commons/schemas/errorResponse.yaml
