properties:
  assessmentId:
    type: string
    description: The specified assessment that the checks have been aggregated for.
  snapshotDate:
    type: string
    format: date-time
    description: The snapshot date for the specified assessment that                                 the checks have been aggregated for.
  groupBy:
    $ref: checkDetailSummaryEnum.yaml
    description: The property that the checks are aggregated by.
  aggregates:
    items:
      $ref: checkDetailSummaryAggregateItem.yaml
    type: array
    description: Summary items for each property value for the group by property.
  checkId:
    type: string
    description: Check ID
type: object
required:
  - assessmentId
  - snapshotDate
  - groupBy
  - aggregates
  - checkId
title: CheckDetailSummaryResponse
description: Represents a best practice assessment check detail summary. [kind/spotinst:cbi:bestPractice:assessment:checkDetail:summary]
