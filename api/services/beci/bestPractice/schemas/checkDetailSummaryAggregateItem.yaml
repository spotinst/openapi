properties:
  groupByValue:
    type: string
    description: The description for the field that the checks are grouped by.
  value:
    type: number
    description: The aggregated sum of values for the group by value.
  currency:
    description: Currency format. Defaults to $.
    default: $
    type: string
    nullable: true
type: object
required:
  - groupByValue
  - value
title: CheckDetailSummaryAggregateItem
description: Represents a best practice assessment check detail summary aggregate item.
