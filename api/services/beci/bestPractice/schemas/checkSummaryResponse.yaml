properties:
  assessmentId:
    type: string
    description: The specified assessment that the checks have been aggregated for.
  snapshotDate:
    type: string
    format: date-time
    description: The snapshot date for the specified assessment that                                 the checks have been aggregated for.
  groupBy:
    $ref: checkSummaryEnum.yaml
    description: The property that the checks are aggregated by.
  aggregates:
    items:
      $ref: checkSummaryAggregateItem.yaml
    type: array
    description: Summary items for each property value for the group by property.
type: object
required:
  - assessmentId
  - snapshotDate
  - groupBy
  - aggregates
title: CheckSummaryResponse
description: Represents a best practice assessment check summary. [kind/spotinst:cbi:bestPractice:assessment:checkSummary]
