properties:
  id:
    type: string
    description: Unique check identifier.
  name:
    type: string
    description: Name of the check (customer facing).
  importance:
    type: string
    enum:
      - low
      - medium
      - high
    description: Identifier that describes the importance or impact of the check.
  category:
    type: string
    enum:
      - usage
      - security
      - cost
      - availability
    description: Identifier of what type of check it is. Grouping mechanism.
  targetedServiceCategory:
    $ref: serviceTypeEnum.yaml
    description: Identifier that describes the targeted service category of the check.
  description:
    type: string
    description: Short description of what the check is doing.
  cloudProviders:
    items:
      type: string
    type: array
    description: The cloud providers to which this check applies.
  infractionCount:
    type: integer
    description: Number of infractions detected for the specified assessment.
  supportsAutoRemediation:
    type: boolean
    description: Provides does this check support auto remediation.
  defaultRemediationTemplate:
    description: Default remediation template id.
    type: string
    nullable: true
  manualRemediationAvailable:
    type: boolean
    description: Displays if manual remediation is available for the check
  remediationEffort:
    description: Identifier that describes the level of effort for manual remediation
    $ref: remediationEffortEnum.yaml
    nullable: true
  source:
    description: Display check source.
    $ref: checkDefintionSource.yaml
    nullable: true
  supportsManagedByOcean:
    type: boolean
    description: Provides does this check service available to be managed by Ocean.
    default: false
  ctaUrl:
    description: Call to action url.
    type: string
    nullable: true
type: object
required:
  - id
  - name
  - importance
  - category
  - targetedServiceCategory
  - description
  - cloudProviders
  - infractionCount
  - supportsAutoRemediation
  - defaultRemediationTemplate
  - manualRemediationAvailable
  - remediationEffort
  - source
title: CheckResponse
description: Represents a best practice assessment check. [kind/spotinst:cbi:bestPractice:assessment:check]
