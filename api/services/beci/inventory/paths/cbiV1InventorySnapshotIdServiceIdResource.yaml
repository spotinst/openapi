get:
  tags:
    - Inventory Resources
  summary: Get Resources
  description: Returns a list of resources processed in the given snapshot ID and service ID.
  operationId: getResourcesByServiceCbiV1InventorySnapshotSnapshotIdServiceServiceIdResourceGet
  parameters:
    - name: snapshotId
      in: path
      required: true
      schema:
        type: string
        format: date-time
        description: Snapshot ID
      description: Snapshot ID
    - name: serviceId
      in: path
      required: true
      schema:
        type: string
        description: Service ID
      description: Service ID
    - name: paginationSize
      in: query
      required: false
      schema:
        type: integer
        description: Number of items to include in the response
      description: Number of items to include in the response
    - name: paginationKey
      in: query
      required: false
      schema:
        type: string
        description: Key used to fetch a specific page of items
      description: Key used to fetch a specific page of items
    - name: accountId
      in: query
      required: false
      schema:
        anyOf:
          - type: string
          - type: 'null'
        description: Spot account ID
      description: Spot account ID
    - name: subAccountIds
      in: query
      required: false
      schema:
        anyOf:
          - type: array
            items:
              type: string
          - type: 'null'
        description: Subaccount IDs
      description: Subaccount IDs
    - name: select
      in: query
      required: false
      schema:
        type: array
        items:
          type: string
        description: List of properties to return for each resource.
        default: []
      description: List of properties to return for each resource.
    - name: includeCommonProperties
      in: query
      required: false
      schema:
        type: boolean
        description: Include the common properties in addition to any properties specified in select
        default: true
      description: Include the common properties in addition to any properties specified in select
    - name: filter
      in: query
      required: false
      schema:
        type: string
        description: Filter expression.
      description: Filter expression.
  responses:
    '200':
      $ref: ../responses/get200cbiV1InventorySnapshotIdServiceIdResource.yaml
    '400':
      content:
        application/json:
          schema:
            $ref: ../../../../commons/schemas/errorResponse.yaml
      description: Invalid request error response. Adjust the request before retrying.
    '404':
      content:
        application/json:
          schema:
            $ref: ../../../../commons/schemas/errorResponse.yaml
      description: Not found error response. The requested resource does not exist.
    '422':
      description: Error response.
      content:
        application/json:
          schema:
            $ref: ../../../../commons/schemas/errorResponse.yaml
