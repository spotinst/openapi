get:
  tags:
    - Inventory Resources
  summary: Get resource detail
  description: Returns a detailed resource response for the provided Service ID.
  operationId: getResourceCbiV1InventorySnapshotSnapshotIdServiceServiceIdResourceDetailGet
  parameters:
    - name: snapshotId
      in: path
      required: true
      schema:
        type: string
        format: date-time
        description: Snapshot ID
      description: Snapshot ID
    - name: serviceId
      in: path
      required: true
      schema:
        type: string
        description: Service ID
      description: Service ID
    - name: accountId
      in: query
      required: false
      schema:
        anyOf:
          - type: string
          - type: 'null'
        description: Spot account ID
      description: Spot account ID
    - name: subAccountId
      in: query
      required: false
      schema:
        anyOf:
          - type: string
          - type: 'null'
        description: Subaccount ID
      description: Subaccount ID
    - name: resourceId
      in: query
      required: false
      schema:
        type: string
        description: Resource ID.
      description: Resource ID.
    - name: includeRawProviderMetadata
      in: query
      required: false
      schema:
        type: boolean
        description: Include the data collected from the cloud provider (as is); structure not guaranteed.
        default: false
      description: Include the data collected from the cloud provider (as is); structure not guaranteed.
  responses:
    '200':
      $ref: ../responses/get200cbiV1InventorySnapshotIdServiceIdResourceDetail.yaml
    '400':
      content:
        application/json:
          schema:
            $ref: ../../../../commons/schemas/errorResponse.yaml
      description: Invalid request error response. Adjust the request before retrying.
    '404':
      content:
        application/json:
          schema:
            $ref: ../../../../commons/schemas/errorResponse.yaml
      description: Not found error response. The requested resource does not exist.
    '422':
      description: Error response.
      content:
        application/json:
          schema:
            $ref: ../../../../commons/schemas/errorResponse.yaml
