get:
  operationId: get_workflow_list
  parameters:
  - $ref: "../parameters/workspace_id.yaml"
  - $ref: "../parameters/owner.yaml"
  - $ref: "../parameters/workspace.yaml"
  responses:
    '200':
      content: {}
      description: Success
  summary: return a list of workflows by fetching all user workflows documents from
    DB
  tags:
  - workflows
parameters: []
post:
  operationId: post_workflow_list
  parameters:
  - $ref: "../parameters/workspace_id.yaml"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../../schemas/WorkflowInput.yaml"
    required: true
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: "../../schemas/WorkflowModel.yaml"
      description: Success
    '201':
      content: {}
      description: Workflow created successfully
    '400':
      content: {}
      description: Request validation error
    '409':
      content: {}
      description: Duplicate workflow
  summary: Create a new workflow by creating a new SSM automation document in AWS
  tags:
  - workflows
  x-codegen-request-body-name: payload
