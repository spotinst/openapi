get:
  description: including workflow description, and tags
  operationId: get_workflow_integration_info
  parameters:
  - $ref: "../parameters/workflow_name.yaml"
  - $ref: "../parameters/version.yaml"
  - $ref: "../parameters/workspace_id.yaml"
  responses:
    '200':
      content: {}
      description: Success
  summary: return Integration instance for the default/selected version of a selected
    workflow,
  tags:
  - workflows
parameters: []
