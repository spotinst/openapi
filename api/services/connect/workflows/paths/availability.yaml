get:
  description: Check if workflow name is available for use.
  operationId: get_workflow_name_validation
  parameters:
  - $ref: "../parameters/workflow_name.yaml"
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: "../../schemas/WorkflowAvailable.yaml"
      description: Success
    '400':
      content: {}
      description: Bad request
  summary: Get Workflow Name Availability
  tags:
  - Connect Workflows
parameters: []
