get:
  tags:
    - Connect Targets
  summary: Gets a list of all aws aliases
  operationId: get_target_account_list
  parameters:
    - name: workspace_id
      in: query
      description: Currently selected Workspace ID
      schema:
        type: string
  responses:
    200:
      description: Success
      $ref: "../responses/targetsGet.yaml"
    400:
        description: "Bad Request"
