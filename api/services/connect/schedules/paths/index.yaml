get:
  Description: List all scheduled workflows for the selected workspace.
  operationId: get_schedules_list
  parameters:
  - $ref: "../parameters/task_id.yaml"
  - $ref: "../parameters/workspace_id.yaml"
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: "../../schemas/ScheduleList.yaml"
      description: Success
    '400':
      content: {}
      description: Bad Request
  summary: List Scheduled Workflows
  tags:
  - Connect Schedules
parameters: []
