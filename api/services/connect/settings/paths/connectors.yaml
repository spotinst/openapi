get:
  description: 'that are TRIGGER_ONLY. For each connector, obtain some basic settings
    from its schema,

    and calculate a boolean status indicating whether it is configured or not.

    A connector is NOT configured if no tenant secret data is stored in the TenantSecretsDb.'
  operationId: get_settings_connector_discovery
  parameters:
  - $ref: "../parameters/workspace_id.yaml"
  responses:
    '200':
      content: {}
      description: Success
  summary: Get a list of all the enabled connectors (integration or resource), excluding
    those
  tags:
  - settings
parameters: []
