get:
  description: 'Endpoint to fetch the list of trigger connectors each containing the
    name, display name, snippet name, and

    configuration status of the respective connector.'
  operationId: get_trigger_connector_discovery
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: "../../schemas/Base_TriggerList.yaml"
      description: Success
    '400':
      content: {}
      description: Bad Request
  summary: List Trigger Connectors
  tags:
  - Connect Settings
parameters: []
