get:
  description: Returns a MapList for the list of trigger connectors.
  operationId: get_trigger_connector_discovery
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: "../../schemas/Base_TriggerList.yaml"
      description: Success
    '400':
      content: {}
      description: Bad Request
  summary: List Trigger Connectors
  tags:
  - Connect Settings
parameters: []
