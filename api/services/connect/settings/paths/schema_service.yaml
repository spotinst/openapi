get:
  description: Get the json schema for the given service.
  operationId: get_setting_schema
  parameters:
  - $ref: "../parameters/service.yaml"
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: "../../schemas/Base_Schema.yaml"
      description: Success
    '400':
      content: {}
      description: Bad Request
  summary: Get Schema
  tags:
  - Connect Settings
parameters: []
