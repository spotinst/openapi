get:
  description: Endpoint to fetch list of API Keys and its values.
  operationId: get_settings_api_keys
  parameters:
  - $ref: "../parameters/disable_redact.yaml"
  - $ref: "../parameters/workspace_id.yaml"
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: "../../schemas/Base_ApiKeys.yaml"
      description: Success
    '400':
      content: {}
      description: Bad Request
  summary: List API Keys
  tags:
  - Connect Settings
parameters: []
