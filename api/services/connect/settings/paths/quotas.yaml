get:
  description: Fetch quotas for organization and provide information on their limits
    and current status.
  operationId: get_quota_list
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: "../../schemas/Base_Quotas.yaml"
      description: Success
    '400':
      content: {}
      description: Bad Request
  summary: Get Quotas
  tags:
  - Connect Settings
parameters: []
