get:
  description: This API is used to fetch Jira authorization URL.
  operationId: get_jira_settings
  parameters:
  - $ref: "../parameters/alias.yaml"
  - $ref: "../parameters/jira_url.yaml"
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: "../../schemas/Base_JiraIntegrationUrl.yaml"
      description: Success
    '400':
      content: {}
      description: Bad Request
  summary: Get Jira Authorization URL
  tags:
  - Connect Settings
parameters: []
