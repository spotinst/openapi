get:
  description: Returns a list of public IPs for customers to whitelist.
  operationId: get_settings_public_i_ps
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: "../../schemas/Base_IPList.yaml"
      description: Success
    '400':
      content: {}
      description: Bad Request
  summary: List Public IPs
  tags:
  - Connect Settings
parameters: []
