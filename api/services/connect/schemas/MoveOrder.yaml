properties:
  move_all_integrations:
    description: If we need to move all integration used in that workflow
    type: boolean
  workflow_name:
    description: Workflow name to move in new workspace
    type: string
  workspace_id:
    description: Workspace id to move the workflow into
    type: string
type: object
