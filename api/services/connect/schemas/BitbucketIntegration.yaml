allOf:
- $ref: ./BaseIntegration.yaml
- properties:
    access_token:
      description: Bitbucket Cloud Access Token for authentication using Access Token.
      type: string
    url:
      description: 'Bitbucket repository URL, ex: Bitbucket personal repo -> `https://bitbucket.org/<username>/<repo-name>`
        for Bitbucket workspace repo -> `https://bitbucket.org/<workspace-name>/<repo-name>`.'
      type: string
  type: object
