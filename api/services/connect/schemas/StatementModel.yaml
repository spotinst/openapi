properties:
  actions:
    items:
      type: string
    type: array
  effect:
    description: ALLOW or DENY
    enum:
    - ALLOW
    - DENY
    example: ALLOW
    type: string
  resources:
    items:
      type: string
    type: array
type: object
