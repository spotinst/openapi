allOf:
- $ref: ./WorkspaceCommonID.yaml
- properties:
    permissions:
      description: List of permissions for that workspace
      items:
        type: string
      type: array
    workspace_id:
      description: Unique workspace ID
      type: string
  type: object
