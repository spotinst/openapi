properties:
  coming_soon:
    description: Whether the connector is coming soon. True for coming soon and false
      if connector is active.
    type: boolean
  connector_type:
    description: Type of connector (Integration, Resource, Cloud).
    type: string
  connectors:
    items:
      description: List of integrations this connector depends on.
      type: string
    type: array
  display_name:
    description: Display name of the connector.
    type: string
  is_configured:
    description: Whether the connector is configured. True if configured and false
      for not configured.
    type: boolean
  is_trigger:
    description: Whether the connector is trigger-based. True for trigger based and
      false for not trigger based.
    type: boolean
type: object
