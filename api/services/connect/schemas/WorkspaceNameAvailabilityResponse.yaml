allOf:
- $ref: ./WorkspaceNameAvailabilityCheck.yaml
- properties:
    available:
      description: Check if workspace name is available or not
      type: boolean
    message:
      description: Response message on availability check
      type: string
  type: object
