properties:
  available:
    description: Tells if workflow name is available for use.(true for available and
      false for unavailable.
    type: boolean
  message:
    description: Helper message.
    type: string
  name:
    description: Name of the workflow to check.
    type: string
type: object
