allOf:
- $ref: ./BaseIntegration.yaml
- properties:
    access_token:
      description: Token used to access the Splunk instance.
      type: string
    splunk_url:
      description: URL of the Splunk instance (e.g. 65.132.55.921). Do not include
        the port number.
      type: string
  type: object
