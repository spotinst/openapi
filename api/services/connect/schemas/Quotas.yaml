properties:
  current_tier:
    description: Current plan tier.
    type: string
  executions:
    allOf:
    - $ref: ./QuotaDetails.yaml
    description: Current plan's execution details.
    type: object
  plan_renew:
    description: Plan renewal date.
    format: date-time
    type: string
  workflows:
    allOf:
    - $ref: ./QuotaDetails.yaml
    description: Current plan's workflow details.
    type: object
type: object
