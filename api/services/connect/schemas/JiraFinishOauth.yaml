allOf:
- $ref: ./ResponseMessage.yaml
- properties:
    status:
      description: Integration status. 'Successful' if the integration was created
        successfully; otherwise, an error message indicating the failure reason.
      type: string
  type: object
