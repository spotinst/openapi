properties:
  created_at:
    description: Time when schedule was created.
    type: string
  created_by:
    description: Tenant ID which created the schedule.
    type: integer
  created_by_username:
    description: Name of the user that created schedule.
    type: string
  enabled:
    description: Displays if schedule is active.
    type: boolean
  rule:
    description: Rule id of schedule
    type: string
  schedule_expression:
    description: CRON expression for schedule.
    type: string
  task_id:
    description: Task id
    type: integer
  workflow:
    description: Name of the workflow.
    type: string
type: object
