properties:
  Content:
    allOf:
    - $ref: ./SSMDoc.yaml
    description: SSM document for the workflow.
    type: object
  Tags:
    description: List of workflow tags.
    items:
      type: string
    type: array
required:
- Content
type: object
