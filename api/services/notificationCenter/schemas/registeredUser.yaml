type: array
items:
  type: object
  properties:
    userEmail:
      type: string
      description: Registered user email. Must be registered to the organization.
      example: "user@example.com"
    subscriptionTypes:
      type: array
      items:
        type: string
        enum: ["email", "console"]
        example: "email"
