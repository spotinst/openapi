type: array
items:
  type: object
  properties:
    userEmail:
      type: string
      example: "user@example.com"
    subscriptionTypes:
      type: array
      items:
        type: string
        enum: ["email", "console"]
        example: "email"
