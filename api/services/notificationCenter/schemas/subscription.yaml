type: array
items:
  type: object
  properties:
    type:
      type: string
      enum: ["email", "webhook", "slack", "sns"]
      example: "email"
    endpoint:
      type: string
      example: "endpoint@example.com"
