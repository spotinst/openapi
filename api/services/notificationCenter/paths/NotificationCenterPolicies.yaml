post:
  summary: "Create Notification Policy"
  description: |
    Creates a notification policy according to a specific configuration.
    <p>Notes:</p>
    <p>Currently supports only Compute policies.</p>

  operationId: "notificationPolicyCreate"
  tags:
    - "Notification Center Service"
  parameters:
    - $ref: "../../../commons/parameters/accountId.yaml"
  requestBody:
    required: true
    content:
      application/json:
        schema:
          allOf:
            - $ref: "../schemas/createRequest.yaml"
  responses:
    200:
       $ref: "../responses/notificationPolicyCreate.yaml"
    400:
      description: "Bad Request"





