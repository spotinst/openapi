get:
  summary: "Get Events Types"
  description: |
    Retrieves all available event types associated with notification policies.
    <p>Notes:</p>
    <p>The results may vary depending on your account's provider.</p> 

  operationId: "EventTypeList"
  tags:
    - "Notification Center Service"
  parameters:
    - $ref: "../../../commons/parameters/accountId.yaml"
  responses:
    200:
      $ref: "../responses/eventTypeList.yaml"
    400:
      description: "Bad Request"





