properties:
  count:
    default: 0
    description: The number of items.
    title: Count
    type: integer
  currentToken:
    anyOf:
    - type: string
    - type: 'null'
    description: 'The current page token. Note: this is an opaque string field.         Do
      not increment/decrement it arithmetically.'
    title: Currenttoken
  items:
    description: An array of objects in this response, the length of which is defined
      by the attribute named 'count'
    items:
      $ref: ./ControlModel.yaml
    title: Items
    type: array
  nextToken:
    anyOf:
    - type: string
    - type: 'null'
    description: 'Token for the next page of results. Note: this is an opaque string
      field.         Do not increment/decrement it arithmetically.'
    title: Nexttoken
  pageSize:
    description: The maximum number of items in the page. The attribute 'item' indicates
      the actual number         of items in the page.
    title: Pagesize
    type: integer
  previousToken:
    anyOf:
    - type: string
    - type: 'null'
    description: 'Token for the previous page of results. Note: this is an opaque
      string field.         Do not increment/decrement it arithmetically.'
    title: Previoustoken
required:
- items
- pageSize
title: PaginatedResponse[ControlModel]
type: object
