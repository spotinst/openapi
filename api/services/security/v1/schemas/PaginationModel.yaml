properties:
  nextKey:
    description: Each API call has a unique token. This is the token for the next
      page of results.
    title: Nextkey
    type: string
  paginationSize:
    description: The maximum number of items in the page. The attribute 'item' indicates
      the actual number of items in the page.
    title: Paginationsize
    type: integer
  previousKey:
    description: Each API call has a unique token. This is the token for the previous
      page of results.
    title: Previouskey
    type: string
title: PaginationModel
type: object
