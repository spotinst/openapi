properties:
  count:
    default: 0
    description: The number of items.
    title: Count
    type: integer
  currentToken:
    anyOf:
    - type: string
    - type: 'null'
    description: Each API call has a unique token. This is the token for the current
      page of results.
    title: Currenttoken
  items:
    description: The array of objects in this response. The length of the array is
      according to the 'count' attribute.
    items:
      $ref: ./RulesModel.yaml
    title: Items
    type: array
  nextToken:
    anyOf:
    - type: string
    - type: 'null'
    description: Each API call has a unique token. This is the token for the next
      page of results.
    title: Nexttoken
  pageSize:
    description: The maximum number of items in the page. The attribute 'item' indicates
      the actual number         of items in the page.
    title: Pagesize
    type: integer
  previousToken:
    anyOf:
    - type: string
    - type: 'null'
    description: Each API call has a unique token. This is the token for the previous
      page of results.
    title: Previoustoken
required:
- items
- pageSize
title: PaginatedResponse[RulesModel]
type: object
