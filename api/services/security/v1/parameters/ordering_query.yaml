description: The field to order the results by.
in: query
name: ordering
required: false
schema:
  allOf:
  - $ref: "../schemas/Ordering.yaml"
  default: -allMisconfigCount
  description: The field to order the results by.
  title: Ordering
