description: Comma-separated AWS account IDs or Azure subscription IDs to include.
  You can have up to 10 IDs.
in: query
name: includeAccounts
required: false
schema:
  anyOf:
  - type: string
  - type: 'null'
  description: Comma-separated AWS account IDs or Azure subscription IDs to include.
    You can have up to 10 IDs.
  title: Includeaccounts
