description: The date the finding was most recently found (YYYY-MM-DD).
example: '1997-11-11'
in: query
name: date
required: false
schema:
  description: The date the finding was most recently found (YYYY-MM-DD).
  format: date
  title: Date
  type: string
