get:
  description: 'A cyber security compliance determines how compliant each asset is,
    against specific security standards.

    This API returns list of all compliances supported in the product.

    A compliance consists of one or more controls.'
  operationId: get_compliances_v1_compliances_get
  parameters:
  - $ref: "../parameters/pageToken_query.yaml"
  - $ref: "../parameters/pageSize_query.yaml"
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: "../schemas/PaginatedResponse_ComplianceModel_.yaml"
      description: Successful Response
    '400':
      content: {}
      description: Bad Request
    '404':
      description: Not found
    '422':
      content:
        application/json:
          schema:
            $ref: "../schemas/HTTPValidationError.yaml"
      description: Validation Error
  summary: Get Compliances
  tags:
  - Compliance
parameters: []
