{
  "name": "openapi",
  "description": "Spot OpenAPI",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git://github.com/spotinst/openapi"
  },
  "license": "Apache-2.0",
  "author": {
    "name": "Spot by Flexera",
    "url": "https://spot.io"
  },
  "scripts": {
    "serve": "yarn build:sections && yarn build:landing && npx http-server build -a 127.0.0.1 -p 8080 -c-1",
    "build:sections": "node scripts/build-sections.js",
    "build:landing": "node scripts/build-landing.js",
    "bundle:redoc": "redoc-cli bundle --template redoc/template.hbs.html --options=redoc/options.json --output build/index.html api/spot.yaml",
    "bundle:swagger": "swagger-cli bundle --outfile build/openapi.json api/spot.yaml",
    "validate": "swagger-cli validate --no-schema api/spot.yaml",
    "clean": "rm -rf build"
  },
  "devDependencies": {
    "@apidevtools/json-schema-ref-parser": "^15.3.5",
    "@redocly/cli": "^2.30.5",
    "prettier": "^2.6.2",
    "redoc-cli": "^0.13.21",
    "swagger-cli": "^4.0.4"
  },
  "engines": {
    "node": ">=12"
  }
}
